package database

import (
	"testing"
)

func TestShouldCreateComment(t *testing.T) {
	// TODO: Setup the user and the thread first
	//db, mock, err := sqlmock.New()
	//if err != nil {
	//	t.Fatalf("an error '%s' was not expected when oopening a stub database connection")
	//}
	//defer db.Close()
}
